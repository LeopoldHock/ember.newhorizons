import { module, test } from 'qunit';
import { setupRenderingTest } from 'ember-qunit';
import { render } from '@ember/test-helpers';
import { hbs } from 'ember-cli-htmlbars';

module('Integration | Component | interactable/button-info', function (hooks) {
  setupRenderingTest(hooks);

  // test('it renders', async function (assert) {
  //   this.set("articleId", "basic-rules/introduction/a-short-story-of-the-solar-system");
  //   await render(hbs`<Interactable::ButtonInfo />`);
  //   assert.dom(this.element.firstChild.firstChild).hasTagName("button");
  // });
});
